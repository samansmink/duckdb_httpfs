# name: test/sql/httpfs.test
# description: test httpfs extension
# group: [httpfs]

# Before we load the extension, this will fail
statement error
SELECT httpfs('Sam');
----
Catalog Error: Scalar Function with name httpfs does not exist!

# Require statement will ensure this test is run with this extension loaded
require httpfs

# Confirm the extension works
query I
SELECT httpfs('Sam');
----
Httpfs Sam 🐥

query I
SELECT httpfs_openssl_version('Michael');
----
<REGEX>:Httpfs Michael, my linked OpenSSL version is OpenSSL.*